<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand fw-bold" href="/">Clearance System</a>
    <div class="ms-auto d-flex align-items-center gap-2">
      <% if (user) { %>
        <span class="text-white small">Welcome, <%= user.fullname %></span>
        <a href="/logout" class="btn btn-light btn-sm">Logout</a>
      <% } else { %>
        <a href="/login" class="btn btn-light btn-sm me-2">Login</a>
        <a href="/register" class="btn btn-outline-light btn-sm">Register</a>
      <% } %>
    </div>
  </div>
</nav>